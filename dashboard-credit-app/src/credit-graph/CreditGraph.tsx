import React, { ReactElement } from "react";

const CreditGraph = (): ReactElement => {
    return (
        <div>
            <h1>Credit Graph</h1>
        </div>
    );
};

export default CreditGraph;
