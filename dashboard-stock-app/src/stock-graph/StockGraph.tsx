import React, { ReactElement } from "react";

const StockGraph = (): ReactElement => {
    return (
        <div>
            <h1>Stock Graph</h1>
        </div>
    );
};

export default StockGraph;
